AI Image Classifier deployed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

{{- if .Values.ingress.enabled }}

Access it at:
  http{{ if .Values.ingress.tls }}s{{ end }}://{{ .Values.ingress.host }}

(make sure that hostname resolves to this cluster's ingress controller)
{{- else if eq .Values.service.type "LoadBalancer" }}

Once an external IP is assigned, access it at the address shown by:
  kubectl get svc {{ include "classify-app.fullname" . }} -n {{ .Release.Namespace }}
{{- else if eq .Values.service.type "NodePort" }}

Access it via the NodePort shown by:
  kubectl get svc {{ include "classify-app.fullname" . }} -n {{ .Release.Namespace }}
{{- else }}

The Service is ClusterIP only, reach it with:
  kubectl port-forward svc/{{ include "classify-app.fullname" . }} -n {{ .Release.Namespace }} 8080:{{ .Values.service.port }}

then open http://localhost:8080
{{- end }}

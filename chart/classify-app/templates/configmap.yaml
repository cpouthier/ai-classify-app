apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "classify-app.fullname" . }}-config
  labels:
    {{- include "classify-app.labels" . | nindent 4 }}
data:
  CLUSTER_NAME: {{ include "classify-app.clusterName" . | quote }}

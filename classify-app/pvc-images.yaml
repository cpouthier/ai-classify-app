apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: images-data
  namespace: ai-demo
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: sc-prod
  resources:
    requests:
      storage: 5Gi
